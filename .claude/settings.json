{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "git status --porcelain | grep -q . && git add -A && git commit -m \"Auto-save: $(git diff --cached --name-only | head -4 | tr '\\n' ',' | sed 's/,$//')\" && git push origin HEAD || true",
            "statusMessage": "Committing and pushing changes…"
          }
        ]
      }
    ]
  }
}
